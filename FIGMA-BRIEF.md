# Бриф проекта — портфолио на основе Figma

**Источник:** Figma, файл «Интерес_работа» (fileKey `uXIe7bEowMDHzjdKOHKuUz`).
Ссылка: https://www.figma.com/design/uXIe7bEowMDHzjdKOHKuUz/%D0%98%D0%BD%D1%82%D0%B5%D1%80%D0%B5%D1%81_%D1%80%D0%B0%D0%B1%D0%BE%D1%82%D0%B0

Собрано через `get_design_context`/`get_screenshot` (Figma MCP) 2026-08-27.
**Верстка и токены взяты 1:1 из Figma, ничего не унаследовано из предыдущих
проектов** — кроме анимационной техники «волна» (`waveStagger`, см. §3), это
было явное указание пользователя после первой (ошибочной) попытки.

## 1. Ключевые node id (fileKey `uXIe7bEowMDHzjdKOHKuUz`)

| Страница | Frame | node id | Высота |
|---|---|---|---|
| Главная | home page | `2235:93600` | 3426 |
| Кейс 001 | case-01 | `1961:28401` | 12233 |
| Кейс 002 | case-02 | `2009:12035` | 8348 |
| Кейс 003 | case-03 | `2022:14228` | 10393 |
| Кейс 004 | case-04 | `2034:15643` | 10310 |
| Кейс 005 | case-05 | `2210:74380` | 7514 |

Обложка + «О проекте» каждого кейса — отдельный под-фрейм `C{N} -01`
(id указан в комментарии соответствующего `page.tsx`).

## 2. Дизайн-токены (сняты из Figma)

- Фон `#fafafa`, текст `#121212`, акцент **синий `#008cff`**.
- Заголовки — **Wix Madefor Display** Bold/SemiBold (есть в Google Fonts,
  используется как есть).
- Текст/лейблы — **Aeonik Pro** Regular/Medium — это ПЛАТНЫЙ шрифт
  (CoType Foundry), файлов нет. Временно заменён на **Manrope** (похожая
  геометричная гротеска, хорошая кириллица) — см. «Недостающее» ниже.
- Канвас — фиксированные **1440px**, по центру (`mx-auto w-[1440px]`).
  В Figma нет мобильной версии макета — адаптив не придумывался.

## 3. Анимация — единственное, что переносится из прошлого проекта

`waveStagger(cols, step)` (`src/lib/gsap.ts`) — диагональный reveal сетки
ячеек, перенесён 1:1 из `Новый проект 3.0`. Подключён к блоку «Кейсы» на
главной (номер+заголовок каждой строки, антидиагональ 5×2).
`ScrollTriggerRefresh.tsx` — тоже общая анимационная инфраструктура
(мульти-refresh ScrollTrigger), не завязана на старую сетку.

## 4. Что собрано кодом 1:1 (реальный DOM, реальный текст)

- Главная целиком: Header, Hero, «о себе» (фото, био, 17 клиентов),
  «кейсы» (список 001–005, обложка 001 развёрнута с иконками — 002–005
  свёрнуты, т.к. в самом Figma-файле для них ещё нет описания/обложки —
  ничего не придумано поверх), Footer (реальные Telegram/Instagram/
  email/телефон, Perm, Russia).
- У каждого из 5 кейсов: обложка (заголовок, номер, декоративная
  композиция) + блок «О проекте» (текст, Позиция/Команда/Клиент) —
  весь текст и все асеты настоящие, из Figma.

## 5. Что временно вставлено скриншотом (не кодом)

Разделы 01/02/03… внутри каждого кейса (всё, что идёт после «О проекте»)
— это **один длинный PNG-скриншот** (`public/cases/case-0N/body.png`),
снятый через `get_screenshot` и обрезанный по высоте обложки. Визуально
1:1 с макетом, но НЕ настоящая вёрстка — без интерактивности, без
реальных `<img>`/текстовых узлов, крупные файлы (1–13 МБ на кейс).

Причина: у каждого кейса это 4–8 сложных секций (иконки, таблицы,
hover-раскладки, коллажи) — перевод каждой в код через `get_design_context`
по кускам возможен, но это большой объём работы по каждому кейсу
отдельно. Сделано осознанно как первый проход «пусть все 6 страниц
существуют и выглядят точно как в Figma» — конвертация конкретных
секций в код is a separate follow-up (скажите, с какого кейса/раздела
начать).

## 6. Недостающее (нужно от пользователя)

- **Файлы шрифта Aeonik Pro** (woff2, начертания Regular 400 и Medium
  500) — сейчас везде, где должен быть Aeonik Pro, стоит Manrope. Пришлите
  файлы — подключу через `next/font/local`, это займёт минуты.
- В `Hero.tsx` в макете буквально написано **«COMUNICATION»** (без
  второй M) — оставлено как в Figma, не исправлено самовольно. Скажите,
  если нужно поправить на «COMMUNICATION».
- Список тегов-клиентов в «о себе» — Yandex-бренды передал точно, но
  проверьте написание («Яндекс Фабрика», «Яндекс Самокаты» и т.д.).

## 7. Известные мелкие расхождения

- Футер: строка «Telegram, Instagram, E-mail» в 180px-контейнере у
  Manrope переносится на 2 строки («E-\nmail») — с Aeonik Pro, скорее
  всего, влезет в одну (в Figma, судя по макету, влезала). Проверить
  после подключения реального шрифта.
