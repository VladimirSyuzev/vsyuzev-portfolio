# Адаптивность

Три раскладки, работаем в колоночной сетке. Планшет и мобайл — **реальный
reflow** (CSS Grid + поток), не масштабирование десктопного холста.

## Брейкпоинты

| Режим | Ширина вьюпорта | Колонок | Tailwind-префикс |
|---|---|---|---|
| **Мобайл** | ≤ 639 | 1 | (база) |
| **Планшет вертикальный** | 640–1023 | 2 | `sm:` |
| **Планшет горизонтальный** | 1024–1199 | 4 | `lg:` |
| **Десктоп** | ≥ 1200 | 8 | `xl:` |

Ориентация планшета определяется шириной, не `orientation`.

## Сетка по брейкпоинтам

| | колонок | ширина колонки | гаттер | поле (margin) |
|---|---|---|---|---|
| Десктоп | 8 | 158px (фикс, холст 1440) | 12px | 44px |
| Планшет-гор | 4 | флюид `1fr` | 12px | 40px |
| Планшет-верт | 2 | флюид `1fr` | 12px | 28px |
| Мобайл | 1 | `100vw − 40` | — | 20px |

- Десктоп: холст 1440 по центру (тип **Center**). Грид-контент 1348px
  (`8 × 158 + 7 × 12`). Выше 1440 — центр + поля по бокам. 1200–1440 —
  лёгкий даунскейл холста `ViewportScale` 0.83–1×.
- Опорные доли десктопа: 2 кол = 328 · 3 кол = 498 · 4 кол = 668 (½) ·
  6 кол = 1008 (¾) · 8 кол = 1348.
- Гаттер 12px на всех брейкпоинтах (из Figma).

## Карта переноса колонок (база — 8)

| Десктоп /8 | Планшет-гор /4 | Планшет-верт /2 | Мобайл /1 |
|---|---|---|---|
| 8 (full) | 4 | 2 | 1 |
| 6 (¾) | 3 | 2 | 1 |
| 4 (½) | 2 | 1 | 1 |
| 3 (⅜) | 2 | 1 | 1 |
| 2 (¼) | 1 | 1 | 1 |

Полноширинные секции (тёмные фоны, мокапы, карусель «варианты»,
Hero-коллаж) — вне сетки, `100vw`; контент внутри центрируется по сетке
своего брейкпоинта.

## Что меняется на планшете/мобайле

- Секции переходят с `absolute` на 1440-холсте → на `CSS Grid` +
  вертикальный поток. Абсолютные `top` заменяются на `gap`/`margin`.
- Скролл-пины и скрабы GSAP на **планшете-верт и мобайле отключаются** —
  отдаём статичный вид. У сложных секций уже есть `reduced`-ветка со
  стопкой — она база для мобильной раскладки.
- Планшет-гор — пины можно оставить, проверять тайминги.
- Мобильная шкала шрифтов — токены через `clamp()`, тело ≥ 16px.
- `<Header>` — мобильное меню (бургер).

## Технические компоненты

- **Токены сетки** — CSS-переменные `--grid-cols / --grid-gutter /
  --grid-margin` в `globals.css`, переопределяются в 3 медиа-запросах
  (mobile-first, `min-width`).
- **Брейкпоинты Tailwind v4**: `sm` 640 (дефолт), `lg` 1024 (дефолт),
  `xl` переопределён на 1200. `md` не используем.
- `<Grid>` / `<Col span={{ base, sm, lg, xl }}>` — обёртки над CSS Grid
  (`src/components/Grid.tsx`).
- `useBreakpoint()` — `useSyncExternalStore` над `matchMedia`, SSR-safe
  (`src/lib/breakpoint.ts`). Возвращает `mobile | tabletP | tabletL |
  desktop`.

## Порядок работ

1. **Планшет** — reflow ~35 секций в 4-кол / 2-кол сетку.
2. **Мобайл** — макеты в Figma → правки → перенос в проект.
